import {describe,it,expect} from "vitest"; import {validatePublicInstagramUrl,detectContentType} from "../src/lib/url";
describe("URL security",()=>{it("accepts reel",()=>expect(detectContentType(validatePublicInstagramUrl("https://www.instagram.com/reel/ABC/?x=1"))).toBe("reel"));it("rejects other domains",()=>expect(()=>validatePublicInstagramUrl("https://example.com/reel/a")).toThrow());it("rejects credentials",()=>expect(()=>validatePublicInstagramUrl("https://x:p@instagram.com/reel/a")).toThrow());it("rejects http",()=>expect(()=>validatePublicInstagramUrl("http://instagram.com/reel/a")).toThrow())})
