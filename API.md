# API
POST /api/media/resolve — validate and resolve a supported public Instagram URL through the configured provider.
POST /api/media/download — accepts a trusted provider mediaId; never an arbitrary URL.
GET /api/media/status/:id — sanitized processing status.
GET /api/health — basic service health.
